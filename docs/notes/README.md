# docs/notes/

What we measured about Apple's private index surfaces — formats, paths, process roles, observed
values. Evidence lives here; the *decisions* it informs live in `docs/adr/`.

Named `YYYYMMDD-topic.md`. Every note opens with a `> **TL;DR:**` line so a reader can stop there.
