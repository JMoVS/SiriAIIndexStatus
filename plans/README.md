# plans/

One markdown file per slice big enough to need one: `YYYYMMDD-feature-name.md`. A plan cites the
ADRs it implements and does not re-litigate design. Archive shipped slices under `plans/archive/`.

Most work in this project is small enough to skip a plan entirely — `BACKLOG.md` is the queue.
